# Install with:
# 1. pip install -r requirements-base.txt
# 2. pip install --index-url https://download.pytorch.org/whl/cpu -r requirements-cpu.txt
#
# Keeping CPU PyTorch separate avoids accidentally pulling CUDA wheels on
# Windows/Linux machines that do not need GPU support.
